class SeedPage:

    # Each seed page has attributes below
    def __init__(self,url, cap1, cap2, need_render):
        self.url = url
        self.cap1 = cap1
        self.cap2 = cap2
        self.need_render = need_render
